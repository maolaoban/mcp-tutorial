## 使用VS Code中的Cline插件

- Installed -> Configure MCP Server

## 设置cline_mcp_settings.json

``` json
{
  "mcpServers": {
    "weather": {
      "command": "node",
      "args": [
        "F:\\code\\mcp-tutorial\\build\\index.js"
      ]
    }
  }
}
```
